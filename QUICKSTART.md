# 🚀 빠른 시작 가이드
## Quick Start Guide - 탄소제로 스마트팜 계산기

이 가이드를 따라하면 5분 안에 시스템을 실행할 수 있습니다!

## 📋 사전 준비

### Python 설치 확인
```bash
python --version
# Python 3.8 이상이어야 합니다
```

Python이 설치되어 있지 않다면:
- Windows: https://www.python.org/downloads/
- Mac: `brew install python3`
- Linux: `sudo apt-get install python3`

## 🎯 3단계 설치

### 1단계: 프로젝트 폴더로 이동
```bash
cd carbon_zero_smartfarm
```

### 2단계: 필요한 패키지 설치
```bash
pip install -r requirements.txt
```

또는 가상환경을 사용하는 경우:
```bash
# 가상환경 생성
python -m venv venv

# 가상환경 활성화
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt
```

### 3단계: 애플리케이션 실행
```bash
streamlit run main.py
```

자동으로 브라우저가 열리며 `http://localhost:8501`에서 앱을 확인할 수 있습니다!

## 🎨 첫 사용 가이드

### 실시간 모니터링 시작하기

1. **📊 실시간 모니터링** 탭 클릭
2. 위치 선택 (예: 서울)
3. 온실 타입 선택 (예: 유리온실)
4. 작물 선택 (예: 토마토)
5. 🔄 새로고침 버튼으로 데이터 업데이트

### 시뮬레이션 실행하기

1. **🔬 시뮬레이션** 탭 클릭
2. 파라미터 입력:
   - 온실 면적: 1000 m²
   - 태양광 용량: 100 kW
   - 히트펌프 용량: 50 kW
3. **🚀 계산 시작** 버튼 클릭
4. 결과 확인 및 다운로드

## 💡 주요 기능 미리보기

### 📈 실시간 모니터링 화면
```
┌─────────────────────────────────────┐
│ 📡 실시간 데이터 모니터링           │
├─────────────────────────────────────┤
│ 📈 주요 지표                        │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐│
│ │태양광│ │전력  │ │순탄소│ │자급률││
│ │발전  │ │소비  │ │배출  │ │      ││
│ └──────┘ └──────┘ └──────┘ └──────┘│
├─────────────────────────────────────┤
│ 🔋 에너지 균형  🌍 탄소 균형        │
│ [차트]          [차트]              │
├─────────────────────────────────────┤
│ ⏰ 24시간 프로파일                  │
│ [시간별 차트]                       │
└─────────────────────────────────────┘
```

### 🔬 시뮬레이션 화면
```
┌─────────────────────────────────────┐
│ 🔬 시뮬레이션 모드                  │
├─────────────────────────────────────┤
│ ⚙️ 시스템 파라미터                 │
│ [입력 폼]                           │
├─────────────────────────────────────┤
│ 📊 계산 결과                        │
│ [KPI 지표]                          │
│ [차트 및 분석]                      │
│ [경제성 분석]                       │
└─────────────────────────────────────┘
```

## 🔍 문제 해결

### 포트가 이미 사용 중인 경우
```bash
streamlit run main.py --server.port 8502
```

### 브라우저가 자동으로 열리지 않는 경우
브라우저를 직접 열고 다음 주소로 접속:
```
http://localhost:8501
```

### 패키지 설치 오류
Python 버전 확인 후 다시 시도:
```bash
python --version
pip install --upgrade pip
pip install -r requirements.txt
```

### 모듈을 찾을 수 없다는 오류
현재 디렉토리 확인:
```bash
pwd  # 또는 Windows: cd
# carbon_zero_smartfarm 폴더에 있어야 합니다
```

## 📱 주요 화면 설명

### 사이드바
- **시스템 정보**: 버전 및 업데이트 날짜
- **설정**: 단위 및 전기요금 설정
- **사용 가이드**: 도움말 및 팁

### 실시간 모니터링 탭
- **주요 지표**: 현재 시간 기준 핵심 지표
- **에너지 균형**: 생산 vs 소비
- **탄소 균형**: 감축 vs 배출
- **24시간 프로파일**: 시간별 상세 분석
- **센서 데이터**: 온도, 습도, CO2, 광도
- **기상 정보**: 날씨 및 일사량

### 시뮬레이션 탭
- **온실 정보**: 면적, 타입, 작물
- **태양광 설비**: 용량, 효율, 날씨 보정
- **히트펌프**: 용량, COP, 운전 모드
- **기타 설비**: 조명, 관수, 환기, 제어
- **분석 기간**: 일일/월간/연간
- **결과 표시**: KPI, 차트, 경제성
- **데이터 다운로드**: CSV, Excel, PDF

## 🎓 사용 팁

### 초보자
1. 먼저 **실시간 모니터링** 탭에서 전체 화면 구성을 파악하세요
2. 기본값으로 **시뮬레이션**을 한 번 실행해보세요
3. 각 파라미터를 조금씩 변경하면서 결과 변화를 관찰하세요

### 중급자
1. 실제 온실 데이터를 입력하여 정확한 분석을 수행하세요
2. 월간/연간 분석으로 장기 트렌드를 파악하세요
3. 경제성 분석으로 투자 타당성을 검토하세요

### 고급자
1. `config.py`에서 업계 기준값을 커스터마이징하세요
2. `calculator.py`의 계산 로직을 수정하여 특수 케이스를 처리하세요
3. `visualizer.py`에서 원하는 차트를 추가하세요

## 🌐 웹 배포 (선택사항)

### Streamlit Cloud 배포
1. GitHub에 코드 업로드
2. https://streamlit.io/cloud 접속
3. 레포지토리 연결
4. 자동 배포!

### 로컬 네트워크 공유
```bash
streamlit run main.py --server.address 0.0.0.0
# 같은 네트워크의 다른 기기에서 접속 가능
# http://[내_IP주소]:8501
```

## 📞 도움이 필요하신가요?

- 📖 README.md: 전체 문서
- 💬 Issue 트래커: 버그 리포트
- 📧 이메일: support@example.com

## ✅ 체크리스트

설치 완료 체크:
- [ ] Python 3.8 이상 설치
- [ ] 패키지 설치 완료 (`pip install -r requirements.txt`)
- [ ] 애플리케이션 실행 성공 (`streamlit run main.py`)
- [ ] 브라우저에서 확인 (`http://localhost:8501`)

첫 실행 체크:
- [ ] 실시간 모니터링 탭 확인
- [ ] 시뮬레이션 실행
- [ ] 결과 다운로드 테스트
- [ ] 사이드바 설정 변경

---

**🎉 축하합니다! 이제 탄소제로 스마트팜 계산기를 사용할 준비가 되었습니다!**

처음 사용하시는 분들은 실시간 모니터링 탭에서 기본값으로 데이터를 확인하신 후, 
시뮬레이션 탭에서 다양한 시나리오를 테스트해보시기 바랍니다.

질문이나 문제가 있으시면 언제든지 문의해주세요! 🌱
